apiVersion: v1
kind: Service
metadata:
  name: dashboards
  namespace: dashboards
spec:
  selector:
    app.kubernetes.io/name: dashboards
  ports:
    - name: http
      port: 80
      targetPort: http
